# Override for VM - uses bind mount for postgres data
# Usage: docker compose -f compose.yaml -f compose.helix.yaml up

volumes:
  usigrabber_helix_pgdata:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/helix/postgres
