{
    // Start from Pyright's recommended rules
  "typeCheckingMode": "recommended",

  // Then override specific settings. I don't think this overwrite really works..
  "failOnWarnings": false,

  "reportMissingTypeStubs": "none"
}
